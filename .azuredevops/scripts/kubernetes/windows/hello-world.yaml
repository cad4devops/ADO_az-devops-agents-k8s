steps:
  - task: CmdLine@2
    displayName: "Run Hello World"
    inputs:
      script: "echo Hello, World!"
  # powershell task to wait 300 seconds
  - task: PowerShell@2
    displayName: "Wait 300 seconds"
    inputs:
      targetType: "inline"
      script: "Start-Sleep -Seconds 300"
